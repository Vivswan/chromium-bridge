# This file is managed by Vivswan/repo-platform.
# Copier uses it to track the template source and version - do not delete.
_commit: 04da02c
_src_path: gh:Vivswan/repo-platform
channel: staging
description: 'Authenticated MCP bridge to your real Chromium browsers (Brave, Chrome):
    Rust native-messaging host + MV3 extension, no debug port'
github_username: Vivswan
modules:
- agents
- bun
- rust
- pages
- release-please
- issue-templates
- pr-title
- auto-assign
pages_build_command: ASTRO_BASE="$PAGES_BASE_PATH" ASTRO_SITE="$PAGES_ORIGIN" bun
    run --cwd src/apps/web build
pages_dist_dir: build/web
pages_install_command: bun install --frozen-lockfile
pages_production: release
pages_setup: bun
pages_staging: true
private: false
project_name: Chromium Bridge
project_slug: chromium-bridge
topics: brave,browser-automation,chrome-extension,mcp,native-messaging,rust
