{
  "_comment": "Copy the `browser-bridge` entry under mcp.servers into your ~/.zcode/cli/config.json. The command path is absolute so it does not depend on your shell PATH. Adjust `command` if you installed the binary elsewhere.",
  "mcp": {
    "servers": {
      "browser-bridge": {
        "command": "/Users/kevin/.browser-bridge/browser-bridge",
        "args": []
      }
    }
  }
}
