{
  "manifest_version": 3,
  "name": "Access-level probe (test fixture)",
  "version": "1.0",
  "description": "Proves Chrome enforces storage.local.setAccessLevel(TRUSTED_CONTEXTS): a content-script-world read of a seeded key must be blocked after the restriction. Used by tests/security_browser_test.ts to verify the browser mechanism the real extension's #32 isolation relies on. Not shipped.",
  "permissions": ["storage", "scripting"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "bg.js" }
}
