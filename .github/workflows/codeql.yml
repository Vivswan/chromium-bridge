# This file is managed by Vivswan/repo-platform.
# Local edits may be replaced during template updates.

name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 1"

permissions:
  contents: read

jobs:
  codeql:
    uses: Vivswan/repo-platform/.github/workflows/reusable-codeql.yml@4f694d4ef5ce170be630bd7e5674174b44407886
    with:
      language: javascript-typescript
    permissions:
      contents: read
      security-events: write
      actions: read
